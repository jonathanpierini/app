import Head from "next/head";

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <Head>
        <title>Mental Wealth</title>
      </Head>
      <main className="min-h-screen bg-white text-gray-800 p-4">{children}</main>
    </>
  );
}
